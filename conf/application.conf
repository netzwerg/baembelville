# This is the main configuration file for the application.
# ~~~~~
application.name=baembelville

# Application mode
# ~~~~~
# Set to dev to enable instant reloading and other development help.
# Otherwise set to prod.
application.mode=dev
bee.application.mode=bee

# Logging
# ~~~~~
application.log=DEBUG

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions
# If you deploy your application to several instances be sure to use the same key !
application.secret=LR5HTLdDFT7k4HtaRv45WcPfi4q0XALUTgfMLxJfFGb7S2RssiYqxX5tTI6s4u9O

# Date format
# ~~~~~
date.format.fr=dd/MM/yyyy

# Database configuration
# ~~~~~ 
# Enable a database engine if needed.
#
# To quickly set up a development database, use either:
#   - mem : for a transient in memory database (H2 in memory)
#   - fs  : for a simple file written database (H2 file stored)
db=mem

# If you need a full JDBC configuration use the following :
bee.db.url=jdbc:cloudbees://baembelville
bee.db.driver=com.cloudbees.jdbc.Driver
bee.db.user=
bee.db.pass=

# JPA Configuration (Hibernate)
# ~~~~~
#
# Specify the custom JPA dialect to use here (default to guess):
bee.jpa.dialect=org.hibernate.dialect.MySQLDialect
jpa.ddl=update